# Changelog

- v1.2.0 - folder opens its items on a single click (switch on mousedown before the tree handles it)
- v1.1.0 - re-pinned the folders pane and fixed folder clicks by selecting the first/library tab directly
- v1.0.0 - reverted folders to stock behavior (pinning removed); folders only show in library view
- v0.9.0 - tried capture-phase folder click listener with a zotero-pane id fallback
- v0.8.0 - tried fixing folder click with a non-capturing click plus a deferred switch
- v0.7.0 - added group right-click menu: rename, color swatches, delete
- v0.6.0 - removed the three-dots button; add-to-group is now right-click only, native menu style
- v0.5.0 - dropped the library row from the panel, click-folder jumps to items, added a per-tab add-to-group menu and sturdier drag
- v0.4.0 - rewrote the layout against the real Zotero 9 DOM, fixing the blank reader
- v0.3.0 - first attempt at pinning the folders pane so it stays visible with a reader open
- v0.2.0 - docked the panel as a left column, hid leftover native tab buttons, added collapse-to-rail toggle
- v0.1.0 - initial vertical tab bar with Edge-style groups and persistence, packaged as an xpi
